---
description: Show the status of background Codex jobs
argument-hint: "[job-id]"
allowed-tools: mcp__plugin_tandem_codex__codex_status
---

Call **codex_status**, passing `job_id` if it is specified in `$ARGUMENTS`.

Show the result concisely. If a job has already completed but the user has not viewed its output yet, mention this and suggest `/tandem:result`.
