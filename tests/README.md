# Тесты

```bash
node tests/regressions.mjs
```

По одному тесту на каждый дефект, найденный в ревью: настоящие `codex` и
`claude` не нужны, всё окружение — заглушки, создаваемые самим тестом во
временном каталоге. Тест `2c` (утечка дескрипторов) требует `/proc` и на
не-Linux молча пропускается.
